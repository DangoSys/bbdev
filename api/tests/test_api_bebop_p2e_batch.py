from _api_test_helper import run_bbdev_case

# You need to set the bitstream path (case dir is derived from it).
# run_bbdev_case("bbdev bebop-p2e --batch '--chip pebble --bitstream /path/to/bitstream.bit --test elf-tests'")
