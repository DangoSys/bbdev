from _api_test_helper import run_bbdev_case

# You need to set the bitstream and build-dir paths.
# run_bbdev_case("bbdev bebop-p2e --batch '--bitstream /path/to/bitstream.bit --build-dir /path/to/build --test elf-tests'")
